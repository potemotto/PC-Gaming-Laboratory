admin/cpu_lists#index
<h1>CPU一覧ページ</h1>
<%= link_to "CPU情報登録ページ", new_admin_cpu_list_path %>
    <table class = "table table-hover">
      <thead>
        <tr>
          <th>メーカー</th>
          <th>CPU名</th>
          <th>リリース年</th>
          <th colspan="3"></th>
        </tr>
      </thead>
      <tbody>
        <% @cpu_lists.each do |cpu_lists| %>
        <tr>
          <td><%= cpu_lists.maker %></td>
          <td><%= cpu_lists.name %></td>
          <td><%= cpu_lists.year_of_release %></td>
          <td><%= link_to "詳細", admin_cpu_list_path(cpu_lists.id) ,class: "btn btn-primary" %></td>
          <td><%= link_to "編集", edit_admin_cpu_list_path(cpu_lists.id) ,class: "btn btn-primary" %></td>
        </tr>
        <% end %>
      </tbody>
    </table>